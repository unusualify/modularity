import{_ as t,c as e,o,a2 as a}from"./chunks/framework.DdOM6S6U.js";const m=JSON.parse('{"title":"Datatable","description":"","frontmatter":{"sidebarPos":5,"sidebarTitle":"Datatable"},"headers":[],"relativePath":"guide/components/others/datatable.md","filePath":"guide/components/others/datatable.md","lastUpdated":1777448653000}'),d={name:"guide/components/others/datatable.md"},i=a(`<h1 id="datatable" tabindex="-1">Datatable <a class="header-anchor" href="#datatable" aria-label="Permalink to &quot;Datatable&quot;">​</a></h1><p><code>Datatable</code> wraps Vuetify&#39;s <code>v-data-table-server</code> with a standard toolbar (title, search, status filter, create button, delete confirmation modal) and flexible row actions. It is built on top of <code>useTable</code> and <code>makeTableProps</code>.</p><div class="note custom-block github-alert"><p class="custom-block-title">NOTE</p><p>This component is an earlier-generation data table. For new work, consider <code>ue-table</code> which uses the same hook but has a more complete feature set. <code>Datatable</code> is retained for existing usages.</p></div><h2 id="usage" tabindex="-1">Usage <a class="header-anchor" href="#usage" aria-label="Permalink to &quot;Usage&quot;">​</a></h2><div class="language-html vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">html</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#B31D28;--shiki-dark:#FDAEB7;--shiki-light-font-style:italic;--shiki-dark-font-style:italic;">datatable</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  name</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;User&quot;</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  :columns</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;columns&quot;</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  :items</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;items&quot;</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  :items-length</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;total&quot;</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  :row-actions</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;rowActions&quot;</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  create-url</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;/users/create&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">/&gt;</span></span></code></pre></div><h2 id="props" tabindex="-1">Props <a class="header-anchor" href="#props" aria-label="Permalink to &quot;Props&quot;">​</a></h2><p>Props are defined by <code>makeTableProps()</code> from <code>@/hooks/useTable</code>. Key props:</p><table><thead><tr><th>Prop</th><th>Type</th><th>Description</th></tr></thead><tbody><tr><td><code>name</code></td><td><code>String</code></td><td>Resource name used for translated labels (add button, list title)</td></tr><tr><td><code>columns</code></td><td><code>Array</code></td><td>Column definitions. Each entry: <code>{ key, title, formatter? }</code></td></tr><tr><td><code>items</code></td><td><code>Array</code></td><td>Current page of row data</td></tr><tr><td><code>itemsLength</code></td><td><code>Number</code></td><td>Total number of items (for server-side pagination)</td></tr><tr><td><code>rowActions</code></td><td><code>Array</code></td><td>Action buttons per row. Each: <code>{ name, icon, color, label? }</code></td></tr><tr><td><code>rowActionsType</code></td><td><code>String</code></td><td><code>&#39;dropdown&#39;</code> renders a <code>v-menu</code>; any other value renders icon buttons</td></tr><tr><td><code>createOnModal</code></td><td><code>Boolean</code></td><td>Shows a create form inside a modal instead of navigating to <code>createUrl</code></td></tr><tr><td><code>editOnModal</code></td><td><code>Boolean</code></td><td>Opens edit form in a modal</td></tr><tr><td><code>noForm</code></td><td><code>Boolean</code></td><td>Hides the create/edit form dialog entirely</td></tr><tr><td><code>createUrl</code></td><td><code>String</code></td><td>URL for the create link button (used when <code>createOnModal</code> is <code>false</code>)</td></tr><tr><td><code>titleKey</code></td><td><code>String</code></td><td>Item property used as the row display title</td></tr></tbody></table><h2 id="toolbar" tabindex="-1">Toolbar <a class="header-anchor" href="#toolbar" aria-label="Permalink to &quot;Toolbar&quot;">​</a></h2><p>The toolbar is rendered inside <code>v-slot:top</code> of <code>v-data-table-server</code>:</p><table><thead><tr><th>Element</th><th>Description</th></tr></thead><tbody><tr><td>Table title</td><td>Displays <code>tableTitle</code> (from <code>useTable</code>) via <code>ue-title</code>. Override with <code>#header</code>.</td></tr><tr><td>Search</td><td><code>v-text-field</code> bound to <code>search</code>; appends <code>mdi-magnify</code> icon</td></tr><tr><td>Status filter</td><td>Dropdown showing <code>mainFilters</code> (count per status). Calls <code>filterStatus(slug)</code>.</td></tr><tr><td>Create button</td><td><code>v-btn-success</code> linking to <code>createUrl</code> or opening the create modal</td></tr><tr><td>Delete modal</td><td><code>ue-modal</code> with a confirmation question and cancel/confirm buttons</td></tr></tbody></table><h2 id="column-formatters" tabindex="-1">Column formatters <a class="header-anchor" href="#column-formatters" aria-label="Permalink to &quot;Column formatters&quot;">​</a></h2><p>Set <code>formatter</code> on a column definition to apply special rendering:</p><table><thead><tr><th><code>formatter</code> value</th><th>Behaviour</th></tr></thead><tbody><tr><td><code>&#39;edit&#39;</code></td><td>Renders the cell value as a clickable <code>v-btn</code> that calls <code>editItem</code></td></tr><tr><td><code>&#39;activate&#39;</code></td><td>Renders the cell value as a clickable <code>v-btn</code> that calls <code>activateItem</code></td></tr><tr><td><code>&#39;switch&#39;</code></td><td>Renders a <code>v-switch</code> (true-value <code>1</code>, false-value <code>0</code>) that calls <code>itemAction(item, &#39;switch&#39;, value, key)</code></td></tr><tr><td>Any other string</td><td>Passed to <code>handleFormatter(formatter, value)</code> which renders via <code>ue-recursive-stuff</code></td></tr></tbody></table><h2 id="row-actions" tabindex="-1">Row actions <a class="header-anchor" href="#row-actions" aria-label="Permalink to &quot;Row actions&quot;">​</a></h2><p>Each action in <code>rowActions</code> is rendered per row:</p><table><thead><tr><th>Key</th><th>Description</th></tr></thead><tbody><tr><td><code>name</code></td><td>Action identifier passed to <code>itemAction(item, action)</code></td></tr><tr><td><code>icon</code></td><td>MDI icon string, or falls back to <code>$name</code> Vuetify alias</td></tr><tr><td><code>color</code></td><td>Icon / button colour</td></tr><tr><td><code>label</code></td><td>Tooltip text (falls back to <code>name</code>)</td></tr></tbody></table><p>On small screens (<code>isSmAndDown</code>) or when <code>rowActionsType === &#39;dropdown&#39;</code>, actions are grouped into a <code>v-menu</code> (activated by <code>$list</code> icon). Otherwise individual icon buttons with tooltips are rendered.</p><h2 id="slots" tabindex="-1">Slots <a class="header-anchor" href="#slots" aria-label="Permalink to &quot;Slots&quot;">​</a></h2><table><thead><tr><th>Slot</th><th>Binding</th><th>Description</th></tr></thead><tbody><tr><td><code>header</code></td><td><code>{ tableTitle }</code></td><td>Replaces the toolbar title area</td></tr><tr><td><code>formDialog</code></td><td>—</td><td>Replaces the built-in create/edit modal (only when \`createOnModal</td></tr></tbody></table><h2 id="emits" tabindex="-1">Emits <a class="header-anchor" href="#emits" aria-label="Permalink to &quot;Emits&quot;">​</a></h2><p>Row actions trigger <code>itemAction(item, action)</code> from <code>useTable</code>, which typically dispatches a Vuex action or calls an API endpoint based on the action name.</p><h2 id="table-options" tabindex="-1">Table options <a class="header-anchor" href="#table-options" aria-label="Permalink to &quot;Table options&quot;">​</a></h2><p>Pagination, sorting, and multi-sort are managed via <code>v-model:options</code> bound to <code>options</code> from <code>useTable</code>. The table is fixed-header, fixed-footer, and sticky with height <code>window.y - 64 - 24 - 59 - 36</code> to fill the viewport.</p>`,24),s=[i];function r(c,n,l,h,p,u){return o(),e("div",null,s)}const k=t(d,[["render",r]]);export{m as __pageData,k as default};
