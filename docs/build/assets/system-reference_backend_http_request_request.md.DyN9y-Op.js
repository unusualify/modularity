import{_ as e,c as a,o as s,a2 as t}from"./chunks/framework.DdOM6S6U.js";const g=JSON.parse('{"title":"Request (abstract)","description":"","frontmatter":{"sidebarPos":3,"sidebarTitle":"Request"},"headers":[],"relativePath":"system-reference/backend/http/request/request.md","filePath":"system-reference/backend/http/request/request.md","lastUpdated":1778363927000}'),i={name:"system-reference/backend/http/request/request.md"},l=t(`<h1 id="request-abstract" tabindex="-1">Request (abstract) <a class="header-anchor" href="#request-abstract" aria-label="Permalink to &quot;Request (abstract)&quot;">​</a></h1><p><strong>File</strong>: <code>src/Http/Requests/Request.php</code><strong>Namespace</strong>: <code>Unusualify\\Modularous\\Http\\Requests</code><strong>Extends</strong>: <code>Illuminate\\Foundation\\Http\\FormRequest</code><strong>Uses</strong>: <code>Unusualify\\Modularous\\Traits\\ManageTraits</code></p><p>An abstract model-aware form request that knows how to merge validation rules across translated attributes and inject model-scoped helpers. Intended as the base class for any request that validates payloads destined for a Modularous Eloquent model.</p><h2 id="construction" tabindex="-1">Construction <a class="header-anchor" href="#construction" aria-label="Permalink to &quot;Construction&quot;">​</a></h2><div class="language-php vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">php</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">public</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> function</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> __construct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">protected</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> $rules </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> [])</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    $this</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">model </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> $this</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-&gt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">model</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><ul><li><code>$rules</code> — optional default schema passed at construction (used by <code>mergeSchemaRules()</code>).</li><li><code>model()</code> comes from <code>ManageTraits</code> and resolves the model class the request is for.</li></ul><h2 id="rule-dispatch" tabindex="-1">Rule dispatch <a class="header-anchor" href="#rule-dispatch" aria-label="Permalink to &quot;Rule dispatch&quot;">​</a></h2><p>Unlike <a href="./base-form-request"><code>BaseFormRequest</code></a>, this class branches only on <code>POST</code> / <code>PUT</code>:</p><table><thead><tr><th>HTTP method</th><th>Rules pipeline</th></tr></thead><tbody><tr><td><code>POST</code></td><td><code>mergeRules( rulesForAll() + rulesForCreate() )</code></td></tr><tr><td><code>PUT</code></td><td><code>mergeRules( rulesForAll() + rulesForUpdate() )</code></td></tr><tr><td>other</td><td><code>[]</code></td></tr></tbody></table><p>Subclasses implement <code>rulesForAll()</code>, <code>rulesForCreate()</code>, and <code>rulesForUpdate()</code> to describe their schema.</p><h2 id="translation-aware-rule-merging" tabindex="-1">Translation-aware rule merging <a class="header-anchor" href="#translation-aware-rule-merging" aria-label="Permalink to &quot;Translation-aware rule merging&quot;">​</a></h2><p><code>mergeRules()</code> performs three steps:</p><ol><li><strong><code>mergeSchemaRules($rules)</code></strong> — placeholder hook for combining constructor-provided <code>$this-&gt;rules</code> with method rules. Currently passes through unchanged.</li><li><strong>Split translated vs. non-translated attributes</strong> using the model&#39;s <code>getTranslatedAttributes()</code> (only if the model uses the <a href="/system-reference/backend/entity-traits/translation/is-translatable"><code>IsTranslatable</code></a> trait).</li><li><strong>Expand translated rules per locale</strong> by calling <code>updateRules()</code> once for each locale in <code>getLocales()</code>, producing keys like <code>title.en</code>, <code>title.tr</code>, etc.</li></ol><p>The result is then run through <a href="#rule-hydration"><code>hydrateRules()</code></a> to expand helper tokens.</p><h2 id="per-locale-rule-expansion" tabindex="-1">Per-locale rule expansion <a class="header-anchor" href="#per-locale-rule-expansion" aria-label="Permalink to &quot;Per-locale rule expansion&quot;">​</a></h2><p>For every translated field, <code>updateRules()</code>:</p><ul><li>Creates per-locale keys (<code>field.en</code>, <code>field.tr</code>, …).</li><li>Drops <code>required*</code> rules and replaces them with <code>nullable</code> when the locale is not active (<code>$localeActive === false</code>).</li><li>Rewrites <code>required_*</code> rules that reference peer fields so they point to the locale-specific key (<code>required_with:name</code> → <code>required_with:name.en</code>).</li><li>Translates <code>unique_translation</code> rules into a closure that queries the translation model, excluding the current record by its <code>translationRelationKey</code> if <code>id</code> is present.</li></ul><h2 id="rule-hydration" tabindex="-1">Rule hydration <a class="header-anchor" href="#rule-hydration" aria-label="Permalink to &quot;Rule hydration&quot;">​</a></h2><p><code>hydrateRules()</code> scans string rules for the token <code>unique_table</code> and rewrites it as:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>unique:&lt;model-table&gt;,&lt;field&gt;[,&lt;id&gt;]</span></span></code></pre></div><p>The <code>id</code> suffix is appended automatically when <code>$this-&gt;id</code> is set, making the rule safe to use for both store and update flows.</p><p>Example — a subclass can write:</p><div class="language-php vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">php</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">public</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> function</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> rulesForAll</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">()</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> array</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    return</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> [</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">        &#39;email&#39;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =&gt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;required|email|unique_table&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    ];</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>and on update it expands to <code>unique:users,email,42</code>.</p><h2 id="translated-message-helper" tabindex="-1">Translated message helper <a class="header-anchor" href="#translated-message-helper" aria-label="Permalink to &quot;Translated message helper&quot;">​</a></h2><p><code>messagesForTranslatedFields($messages, $fields)</code> and its private helper <code>updateMessages()</code> mirror the rule expansion for error messages — they fan each defined message out across every locale, replacing <code>{lang}</code> with the actual locale code.</p><h2 id="authorization" tabindex="-1">Authorization <a class="header-anchor" href="#authorization" aria-label="Permalink to &quot;Authorization&quot;">​</a></h2><p><code>authorize()</code> returns <code>true</code>. Override in a subclass if the action should be guarded.</p><h2 id="known-subclasses-in-this-package" tabindex="-1">Known subclasses in this package <a class="header-anchor" href="#known-subclasses-in-this-package" aria-label="Permalink to &quot;Known subclasses in this package&quot;">​</a></h2><table><thead><tr><th>Subclass</th><th>Purpose</th></tr></thead><tbody><tr><td><a href="./file-request"><code>FileRequest</code></a></td><td>File-library upload payloads keyed by endpoint type</td></tr><tr><td><a href="./oauth-request"><code>OauthRequest</code></a></td><td>OAuth provider validation on callback</td></tr></tbody></table><h2 id="related" tabindex="-1">Related <a class="header-anchor" href="#related" aria-label="Permalink to &quot;Related&quot;">​</a></h2><ul><li><a href="#"><code>ManageTraits</code></a> — resolves <code>$this-&gt;model()</code> (internal plumbing trait)</li><li><a href="/system-reference/backend/entity-traits/translation/is-translatable"><code>IsTranslatable</code></a> — the flag that activates per-locale rule expansion</li><li><a href="/system-reference/backend/entity-traits/translation/has-translation"><code>HasTranslation</code></a> — provides <code>getTranslationModelName()</code> and <code>getTranslationRelationKey()</code> used by the closure-based <code>unique_translation</code> rule</li></ul>`,32),r=[l];function o(n,d,c,h,p,u){return s(),a("div",null,r)}const b=e(i,[["render",o]]);export{g as __pageData,b as default};
