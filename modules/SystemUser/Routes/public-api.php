<?php

use Illuminate\Support\Facades\Route;

/*
|--------------------------------------------------------------------------
| Public API Routes
|--------------------------------------------------------------------------
|
| Here is where you can register public API routes that don't require
| authentication. These routes will be available at /api/v1/public/*
|
*/

Route::group([], function () {
    // Route::get('users/meta', 'UserController@meta');
    // Route::get('users/search', 'UserController@search');
    // Route::get('users/filters', 'UserController@filters');
});
